<Window x:Class="RentACarDesktop.Panel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RentACarDesktop"
        mc:Ignorable="d"
        Title="Panel" Height="450" Width="800">
    <Grid>
        <!--<ListBox x:Name="lbVehicles" Grid.Row="1" BorderThickness="0" 
            ItemsSource="{Binding}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Title}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->
        <ListView x:Name="lbVehicles">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Title" Width="70" DisplayMemberBinding="{Binding Title}"/>
                        <GridViewColumn Header="Model" Width="70" DisplayMemberBinding="{Binding Model}"/>
                        <GridViewColumn Header="Brand" Width="70" DisplayMemberBinding="{Binding Brand}"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
